# VCS / tooling
.git
.gitignore
.claude
.dockerignore

# Kubernetes / Helm / Dockerfiles (not needed inside the image; the Dockerfiles
# under deploy/build are read via `docker build -f`, not copied from context)
deploy

# Docs and local assets
project_docs
README.md
PROJECT_SETUP.md

# Build artifacts / editor cruft
bin
*.out
*.log
